# Story 4.7: Contatos — Lista Unificada e Exportação

## Status: Draft

## Executor Assignment
- **executor:** @dev
- **quality_gate:** @qa
- **quality_gate_tools:** [npm run build, npm run lint, npm run typecheck]

## Story

**Como** gestor de relacionamento,
**quero** ver todos os contatos em uma lista unificada (gerentes de loja + MKT shopping) e poder exportá-los em CSV,
**para** facilitar comunicação em massa, envio de propostas e controle de relacionamento com shoppings.

## Acceptance Criteria

- [ ] **AC-1:** `/dashboard/contacts` exibe lista única com todos os contatos (ambos os tipos: `store_manager` e `shopping_mkt`)
- [ ] **AC-2:** Lista mostra colunas: Nome, Shopping, Tipo (badge), Telefone, E-mail
- [ ] **AC-3:** Coluna "Tipo" com badge: "Gerente de Loja" (azul) | "MKT Shopping" (roxo)
- [ ] **AC-4:** Busca por nome, shopping ou e-mail (filtro local, sem nova query)
- [ ] **AC-5:** Botão "Exportar CSV" (admin only) gera arquivo com todos os contatos visíveis
- [ ] **AC-6:** CSV inclui colunas: Nome, Shopping, Tipo, Telefone, E-mail, Criado em
- [ ] **AC-7:** CSV com encoding UTF-8 BOM para compatibilidade com Excel
- [ ] **AC-8:** Paginação ou virtualização se > 50 registros (evitar scroll infinito)
- [ ] **AC-9:** Viewers veem a lista mas não o botão de exportar
- [ ] **AC-10:** Build, lint e typecheck passam sem erros

## Dev Notes

### Dados da Planilha (referência)
A aba "CONTATOS GERENTES DE LOJA" e "CONTATOS MKT SHOPPING" da planilha do cliente têm:
- Shopping, Nome do Contato, Telefone, E-mail
Esses dados já existem na tabela `contacts` do projeto.

### CSV Export
```ts
const exportCSV = (contacts: Contact[]) => {
  const BOM = '\uFEFF'
  const headers = ['Nome', 'Shopping', 'Tipo', 'Telefone', 'Email', 'Criado em']
  const rows = contacts.map(c => [
    c.name,
    c.shopping_name,
    c.contact_type === 'store_manager' ? 'Gerente de Loja' : 'MKT Shopping',
    c.phone || '',
    c.email || '',
    new Date(c.created_at).toLocaleDateString('pt-BR')
  ])
  const csv = BOM + [headers, ...rows].map(r => r.map(v => `"${v}"`).join(',')).join('\n')
  const blob = new Blob([csv], { type: 'text/csv;charset=utf-8' })
  const url = URL.createObjectURL(blob)
  const a = document.createElement('a')
  a.href = url
  a.download = `contatos-${new Date().toISOString().split('T')[0]}.csv`
  a.click()
}
```

### Layout
- Manter tabs existentes (se houver) OU substituir por lista unificada com filtro de tipo
- Preferir lista unificada com badge de tipo para experiência mais simples
- Botão "Exportar CSV" no canto superior direito (admin only)

## Tasks

- [ ] **Task 1:** Atualizar `/dashboard/contacts/page.tsx` para exibir lista unificada
- [ ] **Task 2:** Adicionar coluna "Tipo" com badge diferenciado
- [ ] **Task 3:** Implementar filtro de busca local (nome, shopping, e-mail)
- [ ] **Task 4:** Implementar `exportCSV` com UTF-8 BOM
- [ ] **Task 5:** Controle de acesso no botão de exportação (admin only)
- [ ] **Task 6:** Paginação para listas longas (> 50 itens)
- [ ] **Task 7:** Run build, lint, typecheck

## File List
- `src/app/dashboard/contacts/page.tsx` — MODIFIED

## Dev Agent Record

### Agent Model Used: —
### Status: Draft

### Change Log:
- 2026-02-27: Story criada por @architect (Aria) com base nas solicitações do cliente

---

## QA Results

### Reviewed By: —
### Gate Decision: PENDING
