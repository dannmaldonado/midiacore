# Story 1.4: Contacts CRUD — CRM de Contatos por Tipo

## Status: Approved

## Executor Assignment
- **executor:** @dev
- **quality_gate:** @architect
- **quality_gate_tools:** [npm run build, npm run lint]

## Story

**Como** gestor de mídia da Lojas Torra,
**quero** ter uma lista centralizada de contatos organizada por tipo (Gerentes de Loja internos vs. MKT de Shopping),
**para** encontrar rapidamente o contato certo de cada shopping sem consultar a planilha.

## Acceptance Criteria

- [ ] **AC-1:** Página `/dashboard/contacts` exibe contatos em duas abas: "Gerentes de Loja" e "MKT Shopping"
- [ ] **AC-2:** Busca funciona por nome, email ou shopping dentro de cada aba
- [ ] **AC-3:** Formulário de criação (`/dashboard/contacts/new`) inclui seleção de tipo (Gerente / MKT Shopping)
- [ ] **AC-4:** Formulário de edição (`/dashboard/contacts/[id]`) pré-preenche dados e permite exclusão
- [ ] **AC-5:** Botão de copiar email e telefone para clipboard (com feedback visual "Copiado!")
- [ ] **AC-6:** CRUD persiste corretamente no Supabase com isolamento por `company_id`
- [ ] **AC-7:** Empty state quando não há contatos na aba ativa
- [ ] **AC-8:** `npm run build` e `npm run lint` passam sem erros

## Dev Notes

### Dependência
- **Bloqueante:** Story 1.1 deve estar completa (campos `contact_type` e `shopping_name` na tabela `contacts`)

### Contexto Arquitetural
- PRD: `docs/prd/midiacore-prd.md` → FR-003 (inclui tabela com 44 contatos reais — 22 gerentes + 22 MKT)
- Arquitetura: `docs/architecture/brownfield-architecture.md` → Seção 12.2

### Estrutura de Páginas a Criar

```
src/app/dashboard/contacts/
├── page.tsx          # Listagem com tabs e busca
├── new/
│   └── page.tsx      # Formulário de criação
└── [id]/
    └── page.tsx      # Formulário de edição + delete
```

### Padrões a Seguir
- Mesmas classes de estilo (Tailwind + `.executive-card`)
- Auth: `useAuth()` de `@/hooks/use-auth`
- Supabase: `createClient()` de `@/lib/supabase/client`

### Campos do Formulário de Contato
```
1. Tipo → contact_type (radio/select: 'store_manager' | 'shopping_mkt')
2. Shopping → shopping_name (TEXT, obrigatório)
3. Nome → name (TEXT, obrigatório)
4. Cargo/Função → role (TEXT, opcional)
5. Empresa → company_name (TEXT, opcional)
6. Telefone → phone (TEXT, opcional)
7. Email → email (TEXT, opcional)
```

### Tabs de Listagem
```tsx
type TabType = 'store_manager' | 'shopping_mkt'

const tabs = [
  { key: 'store_manager', label: 'Gerentes de Loja' },
  { key: 'shopping_mkt',  label: 'MKT Shopping' },
]
```

Filtrar contatos por `contact_type === activeTab` no client-side após carregar todos.

### Botão Copiar (Clipboard API)
```tsx
const copyToClipboard = async (text: string, field: string) => {
  await navigator.clipboard.writeText(text)
  setCopied(field) // estado local
  setTimeout(() => setCopied(null), 2000)
}

// Uso:
<button onClick={() => copyToClipboard(contact.email, `email-${contact.id}`)}>
  {copied === `email-${contact.id}` ? 'Copiado!' : contact.email}
</button>
```

### Layout do Card de Contato
```
[Avatar com iniciais] Nome do Contato
                      Shopping — Cargo
                      📞 Telefone [copiar]
                      ✉️ Email [copiar]
```

### Gotchas
- O campo `contact_type` não existe na interface `Contact` atual — foi adicionado na Story 1.1
- `navigator.clipboard` requer HTTPS em produção — funciona em localhost sem problemas
- A tabela `contacts` já tem `company_name` como campo de texto (nome da empresa externa) — diferente de `companies` (multi-tenant)
- `shopping_name` foi adicionado na Story 1.1 como campo denormalizado para facilitar queries

## Tasks

- [ ] **Task 1: Criar página de listagem com tabs**
  - [ ] 1.1 Criar `src/app/dashboard/contacts/page.tsx`
  - [ ] 1.2 Implementar fetch de todos os contatos por `company_id`
  - [ ] 1.3 Implementar estado de aba ativa (`store_manager` | `shopping_mkt`)
  - [ ] 1.4 Implementar filtro client-side por `contact_type` e busca por nome/shopping/email
  - [ ] 1.5 Implementar card de contato com botões de copiar email e telefone
  - [ ] 1.6 Implementar feedback visual "Copiado!" com timeout
  - [ ] 1.7 Implementar empty state por aba
  - [ ] 1.8 Adicionar botão "Novo Contato" com link para `/new`

- [ ] **Task 2: Criar formulário de novo contato**
  - [ ] 2.1 Criar `src/app/dashboard/contacts/new/page.tsx`
  - [ ] 2.2 Implementar seleção de tipo (radio buttons ou select)
  - [ ] 2.3 Implementar formulário completo com todos os campos
  - [ ] 2.4 Implementar INSERT no Supabase com `company_id` do profile

- [ ] **Task 3: Criar formulário de edição**
  - [ ] 3.1 Criar `src/app/dashboard/contacts/[id]/page.tsx`
  - [ ] 3.2 Implementar fetch do contato por `id`
  - [ ] 3.3 Pré-preencher todos os campos incluindo `contact_type`
  - [ ] 3.4 Implementar UPDATE no Supabase
  - [ ] 3.5 Implementar DELETE com confirmação

- [ ] **Task 4: Verificação**
  - [ ] 4.1 Executar `npm run build` sem erros
  - [ ] 4.2 Executar `npm run lint` sem erros

## Dev Agent Record

### Agent Model Used: -
### Debug Log References: []
### Completion Notes: []

### File List
- `src/app/dashboard/contacts/page.tsx` — NOVO
- `src/app/dashboard/contacts/new/page.tsx` — NOVO
- `src/app/dashboard/contacts/[id]/page.tsx` — NOVO

### Change Log
| Data | Alteração | Autor |
|------|-----------|-------|
| 2026-02-25 | Story criada | River (SM) |
| 2026-02-25 | Validação @po: GO (8/10) — adicionados executor/@dev, quality_gate/@architect | Pax (PO) |
