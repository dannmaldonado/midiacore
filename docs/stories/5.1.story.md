# Story 5.1 — Contatos: Campo Observações/Notas + Fix Email em Gestão de Usuários

## Status
Ready for Review

## Story
**Como** usuário do MidiaCore,
**Quero** registrar observações/notas nos contatos e ver o email correto na tabela de usuários,
**Para que** eu possa manter informações contextuais sobre cada contato e identificar usuários pelo email real.

## Acceptance Criteria

### AC-1: Campo Observações/Notas em Contatos
- [ ] Interface `Contact` em `src/types/index.ts` deve incluir `notes?: string | null`
- [ ] Formulário de **Novo Contato** (`src/app/dashboard/contacts/new/page.tsx`) deve exibir campo "Observações" (textarea, opcional)
- [ ] Formulário de **Editar Contato** (`src/app/dashboard/contacts/[id]/page.tsx`) deve exibir campo "Observações" com valor pré-preenchido
- [ ] Campo `notes` deve ser salvo/atualizado no Supabase (tabela `contacts`)
- [ ] Migração de banco adicionando coluna `notes TEXT NULL` na tabela `contacts`

### AC-2: Fix — Email real exibido na tabela de Usuários
- [ ] `UserManagementTable.tsx` coluna "Email" atualmente exibe `user.id` (linha 243) — deve exibir o email real
- [ ] Criar endpoint `GET /api/users` que retorna profiles enriquecidos com email do Supabase Auth
- [ ] Atualizar tipo `Profile` em `src/types/index.ts` para incluir `email?: string`
- [ ] `UserManagementTable` deve chamar `GET /api/users` para buscar lista de usuários com emails
- [ ] Coluna "Email" na tabela exibe email real ou "—" se não disponível

## Dev Notes

### Contexto Técnico
- Stack: Next.js 14 (App Router), TypeScript, Supabase (Auth + Database)
- Sem ORM — queries diretas via `supabase-js`
- Auth admin via `createClient({ admin: true })` em route handlers

### Observações sobre o Campo Notes em Contatos
- Coluna já PODE existir no banco (verificar antes de migrar)
- Interface `Contact` em `src/types/index.ts:56-68` — adicionar `notes?: string | null`
- Formulários seguem pattern idêntico — adicionar textarea após os campos de telefone/email

### Bug de Email em UserManagementTable
- **Root cause:** `Profile` type não tem `email` (email está no Supabase Auth, não na tabela `profiles`)
- **Linha afetada:** `src/components/dashboard/UserManagementTable.tsx:243` — `{user.id}` ao invés de email
- **Solução recomendada:**
  - `GET /api/users` → chamar `supabaseAdmin.auth.admin.listUsers()` + merge com profiles
  - Retornar array com `{ ...profile, email: authUser.email }`
  - Atualizar tipo `Profile` para incluir `email?: string`

### Arquivos a Modificar
- `src/types/index.ts` — Contact interface (notes) + Profile interface (email)
- `src/app/dashboard/contacts/new/page.tsx` — adicionar campo notes
- `src/app/dashboard/contacts/[id]/page.tsx` — adicionar campo notes
- `src/app/api/users/route.ts` — adicionar GET handler
- `src/components/dashboard/UserManagementTable.tsx` — usar GET /api/users + exibir email
- `supabase/migrations/` — nova migração para coluna notes em contacts

## Tasks

### Task 1 — Migração de banco
- [x] 1.1 Verificar se coluna `notes` já existe na tabela `contacts` via Supabase dashboard ou `\d contacts`
- [x] 1.2 Criar arquivo de migração em `supabase/migrations/` com `ALTER TABLE contacts ADD COLUMN IF NOT EXISTS notes TEXT NULL;`
- [ ] 1.3 Aplicar migração: `npx supabase db push` ou via painel Supabase

### Task 2 — Tipo Contact com campo notes
- [x] 2.1 Adicionar `notes?: string | null` à interface `Contact` em `src/types/index.ts`

### Task 3 — Formulário Novo Contato
- [x] 3.1 Adicionar `notes: ''` ao estado inicial `formData` em `contacts/new/page.tsx`
- [x] 3.2 Adicionar textarea "Observações" no formulário (após bloco telefone/email)
- [x] 3.3 Incluir `notes` no payload do `.insert()` no Supabase

### Task 4 — Formulário Editar Contato
- [x] 4.1 Adicionar `notes: ''` ao estado inicial `formData` em `contacts/[id]/page.tsx`
- [x] 4.2 Pré-preencher `notes` ao carregar o contato no `useEffect`
- [x] 4.3 Adicionar textarea "Observações" no formulário (após bloco telefone/email)
- [x] 4.4 Incluir `notes` no payload do `.update()` no Supabase

### Task 5 — GET /api/users com emails
- [x] 5.1 Adicionar handler `GET` em `src/app/api/users/route.ts`
- [x] 5.2 Handler verifica autenticação + role admin
- [x] 5.3 Chamar `supabaseAdmin.auth.admin.listUsers()` para obter emails
- [x] 5.4 Chamar `supabase.from('profiles').select('*')` para obter profiles
- [x] 5.5 Fazer merge: `profiles.map(p => ({ ...p, email: authUsers.find(u => u.id === p.id)?.email }))`
- [x] 5.6 Retornar array enriquecido

### Task 6 — Fix UserManagementTable
- [x] 6.1 Adicionar `email?: string` à interface `Profile` em `src/types/index.ts`
- [x] 6.2 Atualizar `UserManagementTable` para chamar `GET /api/users` ao invés de query direta ao Supabase
- [x] 6.3 Substituir `{user.id}` por `{user.email || '—'}` na coluna Email (linha ~243)

## Dev Agent Record

### Agent Model Used
claude-haiku-4-5-20251001

### Debug Log References
- Coluna `notes` ausente confirmada em `20260222010000_initial_schema.sql`
- Import `createClient` removido de `UserManagementTable.tsx` após migração para fetch
- TypeScript: 0 erros | Lint: 0 erros

### Completion Notes
- Task 1.3 pendente: migração precisa ser aplicada via `npx supabase db push` (requer acesso ao projeto Supabase)
- Todas as demais tasks implementadas e validadas

### File List
- `supabase/migrations/20260303000000_add_notes_to_contacts.sql` — CRIADO
- `src/types/index.ts` — MODIFICADO (Contact.notes, Profile.email)
- `src/app/dashboard/contacts/new/page.tsx` — MODIFICADO (campo notes)
- `src/app/dashboard/contacts/[id]/page.tsx` — MODIFICADO (campo notes)
- `src/app/api/users/route.ts` — MODIFICADO (GET handler)
- `src/components/dashboard/UserManagementTable.tsx` — MODIFICADO (fetch GET /api/users, email real)

### Change Log
| Data | Mudança |
|------|---------|
| 2026-03-03 | Story criada por @sm (River) |
| 2026-03-03 | Validada e aprovada por @po (Pax) — 8/10 GO |
| 2026-03-03 | Implementada por @dev (Dex) — tsc ✅ lint ✅ |
